{
  "name": "circleci-commitlint",
  "scripts": {
    "docker:build": "docker build -t williamlauze/circleci-commitlint . --no-cache",
    "docker:push": "docker push williamlauze/circleci-commitlint"
  }
}
